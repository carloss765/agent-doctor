# Agent Doctor Landing

Separate Astro landing page for Agent Doctor.

```bash
pnpm --dir landing install
pnpm --dir landing dev
pnpm --dir landing build
```

The page is intentionally static. React is not used because the current experience only needs Astro markup, CSS, and a small canvas animation.
