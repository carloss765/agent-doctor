# Agent Ready does not require environment variables for the MVP.
#
# Add future optional configuration values here using safe placeholders.
# Never commit real secrets, tokens, API keys, or credentials.
